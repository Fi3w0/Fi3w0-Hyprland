if status is-interactive
    # Commands to run in interactive sessions can go here

    # Minecraft server alias
    alias mc "docker exec -it mcserver rcon-cli"
end

fish_add_path /home/fiw/.spicetify
